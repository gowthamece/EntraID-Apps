@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row" aria-labelledby="appTitle">
        <h1 id="appTitle">SharePoint CSOM File Upload</h1>
        <p class="lead">Upload files to SharePoint Online using CSOM with Microsoft Entra ID application permissions (client credentials), without delegated user sign-in.</p>
        <p><a href="@Url.Action("Index", "SharePoint")" class="btn btn-primary btn-md">Go to Upload</a></p>
    </section>
</main>